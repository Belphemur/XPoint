#pragma once

// Host stub for the unconditional <esp_heap_caps.h> include in Epub.cpp.
// All heap_caps call sites there are gated behind BOARD_HAS_PSRAM, which the
// host test does not define, so no symbols are needed.
